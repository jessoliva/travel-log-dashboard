<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="text">
            {{text}}
        </div>
        <div class="meta">
            {{fix_case user.username}} on {{format_date created_at}}
        </div>

    </section>
    {{/each}}
</div>